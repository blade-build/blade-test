# Treat .bat files (Windows batch scripts) with CRLF line endings
*.bat text eol=crlf

# Treat .sh files (Unix shell scripts) with LF line endings
*.sh text eol=lf

# Ensure all other text files use the platform-native EOL unless overridden
* text=auto
